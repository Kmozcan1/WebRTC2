<?xml version="1.0" encoding="utf-8"?>
<layout>

  <data>
    <variable name="mainActivity" type="com.vox.sample.webrtc.MainActivity"/>
    <variable name="serviceInfo" type="android.net.nsd.NsdServiceInfo"/>
  </data>

  <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      android:id="@+id/stream_list_item_layout"
      android:layout_width="match_parent"
      android:layout_height="60dp"
      android:onClick="@{() -> mainActivity.onListClick(serviceInfo)}">

    <ImageView
        android:id="@+id/stream_list_arrow_image"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginEnd="16dp"
        android:scaleType="centerInside"
        android:tint="@color/colorBlueGrey"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_next" />

    <TextView
        android:id="@+id/stream_text_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:ellipsize="end"
        android:gravity="center_vertical"
        android:maxLines="2"
        android:padding="5dp"
        android:textColor="@color/colorMiddleBlack"
        android:textSize="16sp"
        android:textStyle="normal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/stream_list_arrow_image"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="@{serviceInfo.serviceName}" />

  </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
